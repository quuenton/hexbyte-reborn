/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package stardust.app;

import stardust.list.LinkedList;

import static stardust.utilities.StringUtils.join;
import static stardust.utilities.StringUtils.split;
import static stardust.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
